import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main:"#ffae00",
    },
    secondary: {
      main:"#fff",
    },
    background: {
      default:"#000000",
    },
  },
});

export default theme;
